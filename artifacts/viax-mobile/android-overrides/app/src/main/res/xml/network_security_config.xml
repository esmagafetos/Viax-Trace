<?xml version="1.0" encoding="utf-8"?>
<!--
  ViaX:Trace — Network security config

  O backend oficial é HTTPS (https://viax-trace-api.onrender.com), portanto
  todo o tráfego de produção usa TLS por padrão. Esta policy:

  - base-config: BLOQUEIA cleartext globalmente (apps modernos não devem
    aceitar HTTP em produção).
  - domain-config (dev only): permite cleartext apenas para hosts usados
    em desenvolvimento — loopback do aparelho, alias do emulador para o
    host (10.0.2.2) e localhost. Útil para devs que rodam o backend
    local com `flutter run --dart-define=API_BASE=http://10.0.2.2:8080`.
  - trust-anchors: apenas certificados do sistema (sem CAs do usuário,
    para evitar MITM via certificados instalados manualmente).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
